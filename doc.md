需求
给我制作一个网站，可以共享电脑的文件，制作一个文件服务系统
后端用Python fastapi也行，用Java也可以，前端html，创建文件夹 可以添加文件夹，可以添加文件，可以删除，可以预览，可以上传，前端界面，视觉美观，用于在局域网内共享文件，当然也可以配置,公网访问。.
给我完整的答案，实现过程以及完整的代码编写，不需要登陆功能
上传文件可以当前目录上传
```
能够中断上传，继续可以断点续传，不要前端轮询，使用sse 推送
上传文件夹 不是只上传里面的文件，文件夹也需要，需要支持推拽上传
修改代码
```

#优化
###1、文件删除，批量
###2、可以修改并固定存储位置
###3、推拽上传
###4、上传文件夹、文件夹删除
###5、优化后端代码，系统有些卡，以及上传功能避免流量过载可以异步，多线程
###6、修改 存储信息 总空间 100 GB 已用空间 计算中...
#优化
```
1、支持断点续传
2、大文件上传
```

生成项目目录
并实现后端交换,
完成需求代码编写


``
3、主题切换（主题页面以及 <!-- 上传对话框 -->
        <el-dialog title="上传文件" :visible.sync="uploadDialog" width="550px" append-to-body :close-on-click-modal="false">
            <div>）
``
